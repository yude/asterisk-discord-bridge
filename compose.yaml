services:
  discord-bridge:
    build:
      context: .
      dockerfile: Dockerfile.discord-bridge
    restart: always
    env_file:
      - .env
    depends_on:
      - asterisk
    network_mode: host
    environment:
      - TZ=Asia/Tokyo
    volumes:
      - /etc/localtime:/etc/localtime:ro
